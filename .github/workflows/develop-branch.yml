name: Develop Branch Workflow

on:
  push:
    branches:
      - develop   # Only triggers on develop branch

jobs:
  hello-develop:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello from Develop
        run: echo "Hello World from develop branch!"
