name: Multiple Jobs Workflow

on:
  push:
    branches:
      - '**'   # Runs on any branch

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build Step
        run: echo "Building the project..."

  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test Step
